name: lazy_load_indexed_stack_example
description: A new Flutter project.

publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=2.16.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  lazy_load_indexed_stack:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true
